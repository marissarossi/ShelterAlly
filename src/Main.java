import javax.swing.table.DefaultTableModel;

public class Main {
    //static DefaultTableModel model;

    public static void main(){
        // testing
        /*model = PendingApplications.getModel();
        Application app = new Application();
        app.setName("Name");
        app.setAddress("133 Grand St.");
        app.setPhoneNumber("555-5555");
        app.setDogSize("large");
        app.setKidsInHome(false);
        app.setOtherDogs(false);
        app.setHasCats(false);
        app.setEnergyLevel("regular");
        app.setHasYard(true);
        model.addRow(new Object[]{app.getName(), app.getAddress(), app.getPhoneNumber(), app.getDogSize(), app.getKidsInHome(), app.getOtherDogs(), app.getHasCats(), app.getEnergyLevel(), app.getHasYard()});*/
    }
}
